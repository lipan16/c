/*
 * 1-5.cpp
 *
 *  Created on: 2018Äê6ÔÂ22ÈÕ
 *      Author: asus
 */

/*
#include <iostream>

using namespace std;

int main(){
	double a,b,result;
	char op;
	do {
		cout << "pealse intput arithmetic expression: ";
		cin >> a >> op >> b;
		switch (op) {
		case '+':
			result = a + b;
			cout << a << op << b << "=" << result << endl; break;
		case '-':
			result = a - b;
			cout << a << op << b << "=" << result << endl; break;
		case '*':
			result = a * b;
			cout << a << op << b << "=" << result << endl; break;
		case '/':
			result = a / b;
			cout << a << op << b << "=" << result << endl; break;
		case '%':
			result = (int) a % (int) b;
			cout << a << op << b << "=" << result << endl; break;
		}
	} while (a != 0 && b != 0 && op != 0);
}
*/
