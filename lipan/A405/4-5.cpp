/*
 * 4-5.cpp
 *
 *  Created on: 2018年4月25日
 *      Author: asus
 *
 *
 *      实现一个简单的计算器模拟 windows 操作系统中的计算器，系统界面不做强制要求。
 *      实现以下功能：
 *      （1）、能进行实数的加减乘除运算；
 *      （2）、能进行高次方、高次开方运算；
 *      （3）、能进行简单三角运算；
 *
 */


/*
#include <iostream>
#include <cmath>
using namespace std;

#define PI 3.1415927

//double addf(double,double);
//double subf(double,double);
//double mulf(double,double);
//double divf(double,double);
double powf(double,int);
typedef double f(double,double);
f addf,subf,mulf,divf;
f *ff;

int main(){
	cout<<sin(90/180.0*PI)<<endl;
	ff=addf;
	cout<<ff(2.0,3)<<endl;
	ff=subf;
	cout<<ff(2.0,3)<<endl;
	ff=mulf;
	cout<<ff(2.0,3)<<endl;
	ff=divf;
	cout<<ff(2.0,3)<<endl;
	return 0;
}

double addf(double a,double b){
	return a+b;
}
double subf(double a,double b){
	return a-b;
}
double mulf(double a,double b){
	return a*b;
}
double divf(double a,double b){
	return a/b;
}

double powf(double a,int b){
	double c=1;
	for(int i=0;i<b;i++)
		c*=a;
	return c;
}
*/

