/*
 * text3.cpp
 *
 *  Created on: 2018Äê4ÔÂ29ÈÕ
 *      Author: asus
 */

/*
#include <iostream>
using namespace std;

double max(double, double, double = 0, double = 0);
//int gcd(int,int);
//int gcc(int,int);

typedef int f(int,int);
f gcd,gcc;

int main() {
	int a = 10, b = 12;
	cout << max(a, b) << endl;
	cout << max(a, b, 17, 0) << endl;
	cout << gcd(12, 9) << endl;
	cout << gcc(12, 9) << endl;
}

double max(double a, double b, double x, double y) {
	return (x > y ? x : y) > (a > b ? a : b) ? (x > y ? x : y) : (a > b ? a : b);
}


int gcd(int a, int b) {
	int g;
	if (b == 0)
		g = a;
	else
		g = gcd(b, a % b);
	return g;
}
int gcc(int a,int b){
	return a>b?a:b;
}
*/
